import { View, Text, Image, SafeAreaView } from "react-native";
import React from "react";

const Login = () => {
  return (
    <SafeAreaView style={{flex:1, borderColor:'blue', borderWidth:1, position:'relative'}}>
      
    </SafeAreaView>
  );
};

export default Login;
