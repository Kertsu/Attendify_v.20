import { View, Text } from 'react-native'
import React from 'react'
import { COLORS } from '../../../constants/theme'

const GatheringForm = () => {
  return (
    <View style={{flex:1, backgroundColor:COLORS.white}}>
      <Text>GatheringForm</Text>
    </View>
  )
}

export default GatheringForm